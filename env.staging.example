# Copiar a .env.staging (Vite carga .env.[mode] con --mode staging).
#   cp env.staging.example .env.staging
# Editar VITE_API_URL y ejecutar:
#   npm run build:staging

VITE_API_URL=https://carmotapi.gislasas.com/api
